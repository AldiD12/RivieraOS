import { useState, useEffect } from 'react';
import { TrendingUp, Zap, Flame, Crown } from 'lucide-react';
import { motion } from 'framer-motion';

const API_URL = 'http://localhost:5171/api';
const VENUE_ID = 1;

// Counter animation hook
const useCountUp = (end, duration = 2000) => {
  const [count, setCount] = useState(0);
  
  useEffect(() => {
    let startTime;
    let animationFrame;
    
    const animate = (currentTime) => {
      if (!startTime) startTime = currentTime;
      const progress = Math.min((currentTime - startTime) / duration, 1);
      
      setCount(Math.floor(progress * end));
      
      if (progress < 1) {
        animationFrame = requestAnimationFrame(animate);
      }
    };
    
    animationFrame = requestAnimationFrame(animate);
    return () => cancelAnimationFrame(animationFrame);
  }, [end, duration]);
  
  return count;
};

export default function ManagerStaffLeaderboard() {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [currentTime, setCurrentTime] = useState(new Date());

  // Update clock every second
  useEffect(() => {
    const timer = setInterval(() => setCurrentTime(new Date()), 1000);
    return () => clearInterval(timer);
  }, []);

  // Fetch analytics data
  useEffect(() => {
    fetchAnalytics();
    const interval = setInterval(fetchAnalytics, 5000);
    return () => clearInterval(interval);
  }, []);

  const fetchAnalytics = async () => {
    try {
      const url = `${API_URL}/Orders/active/${VENUE_ID}`;
      console.log('Fetching from:', url);
      const response = await fetch(url);
      if (!response.ok) throw new Error(`HTTP ${response.status}`);
      const activeOrders = await response.json();
      console.log('Got orders:', activeOrders.length);
      
      const totalRevenue = activeOrders.reduce((sum, o) => sum + (o.totalAmount || 0), 0);
      const totalOrders = activeOrders.length;
      
      setData({
        overview: { 
          totalRevenue, 
          totalOrders, 
          avgOrderValue: totalOrders > 0 ? totalRevenue / totalOrders : 0, 
          completionRate: 85, 
          waiterRevenue: 0, 
          sunbedRevenue: 0 
        },
        waiterPerformance: [],
        topSellingItems: [],
        hourlyBreakdown: [],
        recentOrders: []
      });
      setLoading(false);
    } catch (error) {
      console.error('Error:', error);
      setData({ 
        overview: { totalRevenue: 0, totalOrders: 0, avgOrderValue: 0, completionRate: 0, waiterRevenue: 0, sunbedRevenue: 0 }, 
        waiterPerformance: [], 
        topSellingItems: [], 
        hourlyBreakdown: [], 
        recentOrders: [] 
      });
      setLoading(false);
    }
  };
  };

  // Animated counters - MUST be called before any conditional returns
  const animatedRevenue = useCountUp(data?.overview?.totalRevenue || 0, 2000);
  const animatedOrders = useCountUp(data?.overview?.totalOrders || 0, 1500);

  if (loading || !data) {
    return (
      <div className="min-h-screen bg-black flex items-center justify-center">
        <div className="text-emerald-400 text-xl font-mono animate-pulse">Loading...</div>
      </div>
    );
  }

  const { overview, waiterPerformance, topSellingItems, hourlyBreakdown, recentOrders } = data;
  
  // Mock growth percentage
  const growthPercent = 12;
  const occupancy = 85;

  return (
    <div className="min-h-screen bg-black text-white p-4">
      {/* HEADER - Fixed Layout */}
      <div className="mb-6">
        <div className="flex flex-col gap-2">
          {/* Time - Pulsing */}
          <motion.div 
            className="text-3xl font-mono font-black tabular-nums text-white"
            animate={{ opacity: [1, 0.7, 1] }}
            transition={{ duration: 2, repeat: Infinity }}
          >
            {currentTime.toLocaleTimeString('en-US', { 
              hour: '2-digit', 
              minute: '2-digit',
              hour12: false 
            })}
          </motion.div>
          
          {/* Label */}
          <div className="text-xs font-mono uppercase tracking-[0.3em] text-zinc-500">
            Riviera Live • Money Mode
          </div>
        </div>
      </div>

      {/* THE HERO - Revenue Card with Glow */}
      <motion.div 
        className="bg-gradient-to-t from-emerald-900/20 to-transparent border border-emerald-800/30 rounded-lg p-6 mb-6"
        initial={{ scale: 0.95, opacity: 0 }}
        animate={{ scale: 1, opacity: 1 }}
        transition={{ duration: 0.5 }}
      >
        <div className="flex items-center justify-between mb-2">
          <div className="text-xs font-mono uppercase tracking-wider text-emerald-500">
            Total Revenue
          </div>
          {/* Growth Badge */}
          <div className="flex items-center gap-1 bg-emerald-500/20 border border-emerald-500/30 rounded-full px-2 py-1">
            <TrendingUp className="w-3 h-3 text-emerald-400" />
            <span className="text-xs font-mono font-bold text-emerald-400">
              ▲ {growthPercent}%
            </span>
          </div>
        </div>
        
        {/* The Money Shot - Glowing Number */}
        <div 
          className="text-6xl font-mono font-black tabular-nums text-emerald-400 mb-4"
          style={{ textShadow: '0 0 20px rgba(52, 211, 153, 0.5)' }}
        >
          €{animatedRevenue.toLocaleString()}
        </div>

        {/* The Mountain Chart */}
        {hourlyBreakdown.length > 0 && (
          <div className="relative h-24">
            <svg className="w-full h-full" preserveAspectRatio="none">
              <defs>
                <linearGradient id="areaGradient" x1="0%" y1="0%" x2="0%" y2="100%">
                  <stop offset="0%" stopColor="rgb(52, 211, 153)" stopOpacity="0.3" />
                  <stop offset="100%" stopColor="rgb(52, 211, 153)" stopOpacity="0" />
                </linearGradient>
              </defs>
              
              {/* Area Fill */}
              <path
                d={(() => {
                  const data = hourlyBreakdown.slice(-12);
                  const maxRev = Math.max(...data.map(h => h.revenue), 1);
                  const width = 100 / data.length;
                  
                  const points = data.map((h, i) => {
                    const x = i * width;
                    const y = 100 - (h.revenue / maxRev * 100);
                    return `${x},${y}`;
                  }).join(' ');
                  
                  return `M 0,100 L ${points} L 100,100 Z`;
                })()}
                fill="url(#areaGradient)"
                vectorEffect="non-scaling-stroke"
              />
              
              {/* Line */}
              <polyline
                points={(() => {
                  const data = hourlyBreakdown.slice(-12);
                  const maxRev = Math.max(...data.map(h => h.revenue), 1);
                  const width = 100 / data.length;
                  
                  return data.map((h, i) => {
                    const x = i * width;
                    const y = 100 - (h.revenue / maxRev * 100);
                    return `${x},${y}`;
                  }).join(' ');
                })()}
                fill="none"
                stroke="rgb(52, 211, 153)"
                strokeWidth="2"
                vectorEffect="non-scaling-stroke"
              />
            </svg>
          </div>
        )}
      </motion.div>

      {/* QUAD GRID - Gamified Colors */}
      <div className="grid grid-cols-2 gap-3 mb-6">
        {/* Orders - Electric Blue */}
        <motion.div 
          className="bg-zinc-900 border border-zinc-800 rounded-lg p-4"
          initial={{ y: 20, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: 0.2 }}
        >
          <div className="flex items-center gap-2 mb-2">
            <Zap className="w-4 h-4 text-cyan-400" />
            <div className="text-xs font-mono uppercase tracking-wider text-zinc-500">
              Active
            </div>
          </div>
          <div className="text-4xl font-mono font-black tabular-nums text-cyan-400">
            {animatedOrders}
          </div>
        </motion.div>

        {/* Avg Spend */}
        <motion.div 
          className="bg-zinc-900 border border-zinc-800 rounded-lg p-4"
          initial={{ y: 20, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: 0.3 }}
        >
          <div className="text-xs font-mono uppercase tracking-wider text-zinc-500 mb-2">
            Avg. Spend
          </div>
          <div className="text-4xl font-mono font-black tabular-nums text-white">
            €{overview.avgOrderValue.toFixed(0)}
          </div>
        </motion.div>

        {/* Occupancy - Warning Orange if high */}
        <motion.div 
          className="bg-zinc-900 border border-zinc-800 rounded-lg p-4"
          initial={{ y: 20, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: 0.4 }}
        >
          <div className="text-xs font-mono uppercase tracking-wider text-zinc-500 mb-2">
            Capacity
          </div>
          <div className={`text-4xl font-mono font-black tabular-nums ${
            occupancy > 80 ? 'text-orange-400' : 'text-white'
          }`}>
            {occupancy}%
          </div>
        </motion.div>

        {/* Completion Rate */}
        <motion.div 
          className="bg-zinc-900 border border-zinc-800 rounded-lg p-4"
          initial={{ y: 20, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: 0.5 }}
        >
          <div className="text-xs font-mono uppercase tracking-wider text-zinc-500 mb-2">
            Done
          </div>
          <div className="text-4xl font-mono font-black tabular-nums text-white">
            {overview.completionRate.toFixed(0)}%
          </div>
        </motion.div>
      </div>

      {/* GAMIFIED LEADERBOARDS */}
      <div className="grid grid-cols-2 gap-6 mb-6">
        {/* Top Waiters - Crown for #1 */}
        <div>
          <div className="text-xs font-mono uppercase tracking-[0.3em] text-zinc-600 mb-3">
            🏆 Top Waiters
          </div>
          <div className="space-y-2">
            {waiterPerformance.length === 0 ? (
              <div className="text-zinc-700 text-xs">No data</div>
            ) : (
              waiterPerformance.slice(0, 3).map((waiter, index) => (
                <motion.div 
                  key={waiter.userId} 
                  className={`flex items-center justify-between p-2 rounded ${
                    index === 0 ? 'bg-yellow-500/10 border border-yellow-500/30' : ''
                  }`}
                  initial={{ x: -20, opacity: 0 }}
                  animate={{ x: 0, opacity: 1 }}
                  transition={{ delay: 0.6 + index * 0.1 }}
                >
                  <div className="flex items-center gap-2">
                    {index === 0 && <Crown className="w-4 h-4 text-yellow-500" />}
                    <span className="text-zinc-600 font-mono text-sm">#{index + 1}</span>
                    <span className="text-white text-sm font-medium">{waiter.name}</span>
                  </div>
                  <span className="text-emerald-400 font-mono text-sm font-bold">
                    €{waiter.revenue.toFixed(0)}
                  </span>
                </motion.div>
              ))
            )}
          </div>
        </div>

        {/* Top Sellers - Fire for #1 */}
        <div>
          <div className="text-xs font-mono uppercase tracking-[0.3em] text-zinc-600 mb-3">
            🔥 Hot Items
          </div>
          <div className="space-y-2">
            {topSellingItems.length === 0 ? (
              <div className="text-zinc-700 text-xs">No data</div>
            ) : (
              topSellingItems.slice(0, 3).map((item, index) => (
                <motion.div 
                  key={item.productId} 
                  className="flex items-center justify-between"
                  initial={{ x: -20, opacity: 0 }}
                  animate={{ x: 0, opacity: 1 }}
                  transition={{ delay: 0.6 + index * 0.1 }}
                >
                  <div className="flex items-center gap-2">
                    {index === 0 && <Flame className="w-4 h-4 text-orange-500" />}
                    <span className="text-zinc-600 font-mono text-sm">#{index + 1}</span>
                    <span className="text-white text-sm font-medium truncate">{item.name}</span>
                  </div>
                  <span className="text-white font-mono text-sm font-bold">
                    {item.quantitySold}×
                  </span>
                </motion.div>
              ))
            )}
          </div>
        </div>
      </div>

      {/* LIVE FEED */}
      <div>
        <div className="text-xs font-mono uppercase tracking-[0.3em] text-zinc-600 mb-3">
          📡 Live Feed
        </div>
        <div className="space-y-2">
          {recentOrders.length === 0 ? (
            <div className="text-zinc-700 text-xs">No orders yet</div>
          ) : (
            recentOrders.slice(0, 5).map((order, index) => (
              <motion.div 
                key={order.id} 
                className="flex items-center justify-between text-sm bg-zinc-900/50 p-2 rounded"
                initial={{ x: -20, opacity: 0 }}
                animate={{ x: 0, opacity: 1 }}
                transition={{ delay: 0.8 + index * 0.05 }}
              >
                <div className="flex items-center gap-3">
                  <span className="text-zinc-600 font-mono text-xs">
                    {new Date(order.createdAt).toLocaleTimeString('en-US', { 
                      hour: '2-digit', 
                      minute: '2-digit',
                      hour12: false 
                    })}
                  </span>
                  <span className="text-white truncate text-xs">
                    {order.items.map(i => `${i.quantity}× ${i.name}`).join(', ')}
                  </span>
                </div>
                <span className={`text-xs font-mono uppercase font-bold ${
                  order.status === 2 ? 'text-emerald-400' : 
                  order.status === 1 ? 'text-yellow-400' : 'text-cyan-400'
                }`}>
                  {order.status === 0 ? 'NEW' : order.status === 1 ? 'PREP' : 'DONE'}
                </span>
              </motion.div>
            ))
          )}
        </div>
      </div>

      {/* Footer Stats */}
      <div className="mt-6 pt-4 border-t border-zinc-800 flex items-center justify-between text-xs">
        <div>
          <span className="text-zinc-600 font-mono">Waiter: </span>
          <span className="text-emerald-400 font-mono font-bold">€{overview.waiterRevenue.toFixed(0)}</span>
        </div>
        <div>
          <span className="text-zinc-600 font-mono">Sunbed: </span>
          <span className="text-cyan-400 font-mono font-bold">€{overview.sunbedRevenue.toFixed(0)}</span>
        </div>
      </div>
    </div>
  );
}
